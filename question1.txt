//TODO Changer les calculs pour une forme matricielle vectorielle 
//TODO Mettre en plus de détail comment effectuer le calcul des gradients de chaque layer (chain rule)



//Soit un réseau qui contient M couches 

RÉPÉTER: 

	POUR CHAQUE exemple FAIRE

		POUR CHAQUE noeud j de la couche d'entrée du réseau FAIRE 
			a_j_ <- item j dans le vecteur d'entrée de l'exemple

		POUR l=2 à M FAIRE
			in_i_ <- Sum_j_ (W_j,i_ * a_j_)
			a_i_ <- g(in_i_)

		POUR CHAQUE noeud i de la couche sortie FAIRE
			// Soit y_i_ la sortie désirée pour le noeud i et a_i_ la sortie obtenue 
			delta_i_ <- g'(in_i_) * (y_i_ - a_i_)

		POUR l=M-1 à 1 FAIRE
			POUR CHAQUE noeud j de la couche l FAIRE 
				delta_j_ <- g'(in_j_) * Sum_i_ (W_j,i_ * delta_i_)
				POUR CHAQUE noeud i de la couche l+1 FAIRE 
					W_j,i_ <- W_j,i_ + alpha * a_j_ * delta_i_ 

JUSQU'À ce que le critère d'arrêt soit satisfait	
